# TypeScript 7 no longer exposes the JavaScript compiler API consumed by
# typescript-eslint. The lint scripts provide the pinned TypeScript 6 alias to
# those packages; do not make npm resolve their incompatible peer to TS 7.
legacy-peer-deps=true
